# harbor-values.yaml

# 1. 외부 노출 및 Ingress 설정 (Traefik)
expose:
  type: ingress
  tls:
    enabled: true
    certSource: auto  # Harbor 내부용 인증서 자동 생성
  ingress:
    hosts:
      core: harbor.dev.com  # [수정] 접속할 도메인
    className: "traefik"      # [수정] Traefik Ingress Class 지정
    annotations:
      # Traefik HTTPS 라우팅 및 업로드 용량 제한 해제
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
      traefik.ingress.kubernetes.io/router.tls: "true"
      ingress.kubernetes.io/proxy-body-size: "0"
      nginx.ingress.kubernetes.io/proxy-body-size: "0"

# 2. 외부 접속 URL (필수)
# 위 Ingress host와 프로토콜(https)을 맞춰야 합니다.
externalURL: https://harbor.dev.com

# 3. 데이터 저장소 설정
persistence:
  enabled: true
  resourcePolicy: "keep"
  persistentVolumeClaim:
    registry:
      storageClass: "nfs-client"
      accessMode: ReadWriteOnce
      size: 20Gi
    jobservice:
      storageClass: "nfs-client"
      accessMode: ReadWriteOnce
      size: 1Gi
    database:
      storageClass: "nfs-client"
      accessMode: ReadWriteOnce
      size: 2Gi
    redis:
      storageClass: "nfs-client"
      accessMode: ReadWriteOnce
      size: 1Gi
    trivy:
      storageClass: "nfs-client"
      accessMode: ReadWriteOnce
      size: 5Gi

# 4. 관리자 비밀번호 설정
harborAdminPassword: "Harbor12345"

# 5. 내부 DB 비밀번호 (보안 권장)
database:
  internal:
    password: "postgres_password"

# 6. 내부 Redis 비밀번호 (보안 권장)
redis:
  internal:
    password: "redis_password"
